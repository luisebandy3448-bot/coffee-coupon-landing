# Cloudflare Workers 配置文件
name = "coffee-coupon-landing"
main = "functions/tiktok-event.js"
compatibility_date = "2024-01-01"

# 环境变量（本地开发使用，生产环境在 Cloudflare Dashboard 中设置）
[vars]
# TIKTOK_ACCESS_TOKEN 需要在 Cloudflare Dashboard 中设置，不要在这里硬编码

# 生产环境配置
[env.production]
name = "tiktok-events-api"
